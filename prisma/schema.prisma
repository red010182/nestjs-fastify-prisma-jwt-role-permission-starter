generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL_CAR_INSURANCE")
}

model User {
  id       Int     @default(autoincrement()) @id
  name     String?
  role     String?
  account  String? @unique
  password String?
}
